# Binance API Credentials
# Get your API keys from: https://www.binance.com/en/my/settings/api-management
#
# IMPORTANT SECURITY NOTES:
# 1. NEVER share these keys with anyone
# 2. NEVER commit .env file to git (it's in .gitignore)
# 3. Enable IP whitelist in Binance for extra security
# 4. Only enable "Futures Trading" permission (no withdrawal)
#
# For TESTNET (practice with fake money):
# Get testnet keys from: https://testnet.binancefuture.com/

# Binance API Key (starts with "xxx...")
BINANCE_API_KEY=your_api_key_here

# Binance API Secret (long string)
BINANCE_API_SECRET=your_api_secret_here

# Optional: Telegram notifications (for alerts)
# TELEGRAM_BOT_TOKEN=your_bot_token
# TELEGRAM_CHAT_ID=your_chat_id
